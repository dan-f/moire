export interface Theme {
  colors: {
    foreground: string;
    foregroundSecondary: string;
    background: string;
    backgroundRaised: string;
    backgroundRecessed: string;
    stream: string[];
  };
}
